#ifndef _PRINTF_
#define _PRINTF_
#include <unistd.h>/*included for the purpose of using the wite function*/
#include <string.h>/*included for the purpose of using strlen()*/
#include <stdarg.h>/*included for the variadic macros*/

int _printf(const char *format, ...);


#endif
